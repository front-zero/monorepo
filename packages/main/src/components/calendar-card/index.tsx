import { Card } from "@ht/components";
import Calendar from "@ht/components/src/ui/calendar";

const CalendarCard = () => {
    return (
        <Card className="calendar-card p-3.8">
            <Calendar />
        </Card>
    );
};

export default CalendarCard;
